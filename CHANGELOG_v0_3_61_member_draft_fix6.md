# CHANGELOG v0.3.61-member-draft-fix6

目的：fix5を対象に、機能棚卸し監査で見つかった「進行不能ではないが機能不一致になる箇所」を修正。

## 修正

- エンディング後「このまま続ける」が `normalizeState()` により `maxTurn:50` へ戻される問題を修正。
  - `state.maxTurn = Math.max(50, Number(state.maxTurn || 50));` に変更し、延長活動の最大ターンを保持。
- フェス情報パネルのライブ評価条件を「過去に一度でも達成」ではなく「直近ライブ結果」で判定するよう修正。
  - GRAND UNDER FESの内部判定が最終/直近ライブ結果を見るため、表示側もそれに合わせた。
  - 表示文言を「直近A評価以上」「直近総合80以上」に変更。
- 未使用の旧通常ターン表示文言に残っていた最低条件テキストを、誤解が少ない表記へ調整。

## 確認

- `node --check game.js` OK。
- データ整合性監査：バンド22 / スキル19 / イベント26 / 加入候補36 / サポート14、参照欠けなし。
- 初回導線VM smoke：1T作詞→募集→2T未完成曲→練習→5T初ライブ→6Tまで通過。
- エンディング後継続：`maxTurn` が70として保持されることを確認。
